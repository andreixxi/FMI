HTML
-taguri semantice (header, nav, section, article, aside)
     div, span, h1-h6, p, img, ol, ul, a, table(nu pentru aliniere!!!), iframe cu ancore atasate
     meta, base

CSS
-selectori: id, clasa, tag, elementContinut, elementCopil, 
               al n-lea copil de tipul unui tag (:nth-of-type)
, pseudoclasa (:hover) 
   -proprietati: width, height, color, background, text-align, font, vertical-align
		 border, padding, margin,  visibility, 
   
-meniu drop-down
-galerie de elemente figure continand  figcaption
-responsive: %(width) , em (font-size), media-query (@media) 
-position(relative, absolute, sticky),z-index
-transform, transition, animation
-display- inclusiv inline-block 
-stilizare flexbox (display)



Js-codul va fi SEPARAT de cel HTML (in fisier extern sau  script in document dar nu inline-pe atribut eveniment!!!)

  -modificare  de proprietati

  -modificare  a stilului unui element 
  
  -modificare  eveniment (ex: el.onclick=..)

  -selectare dupa id,tag,clasa,selectorCSS

  -creare si stergere element (createElement si removeChild)
  